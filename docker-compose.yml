name: ctrld-dev

services:
  backend:
    build:
      context: .
      target: backend-builder
    command: go run ./cmd/ctrld -config /etc/ctrld/config.yaml
    ports:
      - "8443:8443"
    volumes:
      - .:/app
      - ctrld-data:/var/lib/ctrld
    environment:
      - CTRLD_SERVER_PORT=8443
      - CTRLD_LOG_LEVEL=debug
      - CTRLD_LOG_FORMAT=pretty
    depends_on:
      - frontend

  frontend:
    image: node:24-alpine
    working_dir: /app/web
    command: npm run dev
    ports:
      - "3000:3000"
    volumes:
      - ./web:/app/web
      - web-modules:/app/web/node_modules
    environment:
      - NEXT_TELEMETRY_DISABLED=1
      - NEXT_PUBLIC_API_URL=http://backend:8443

volumes:
  ctrld-data:
  web-modules:
